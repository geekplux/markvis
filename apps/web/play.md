---
layout: page
sidebar: false
title: Play
---

# Play

Left fence, right figure. Same parser and SVG renderer as the CLI.

The product UI lives in `apps/playground` (Vite, no backend). Local:

```
pnpm --filter playground dev
```

Deep link from the gallery: `/play?example=01-bar-basic`. Open in gallery from the playground: `/examples?id=01-bar-basic`.

