{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Public VitePress site",
  "scripts": {
    "dev": "pnpm --filter playground run build:embed && vitepress dev",
    "build": "pnpm --filter playground run build:embed && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
