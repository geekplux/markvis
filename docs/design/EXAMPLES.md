# EXAMPLES — same family as home

Not a second yellow hero. Not a white gallery. Yellow nav, dark page, paper cards so the figures read, light footer.

Charts stay folio unless a theme chip is on. Do not restyle marks.

## Instruction — Coder

Keep type filter, theme filter, and click-to-fence. Change chrome only. Do not touch `/`.

| role | value |
| --- | --- |
| nav field | `#FFDB2A` |
| page | `#0E1312` |
| card | `#EDEBE5` |
| footer | `#F8F8F6` |
| rule | `rgba(237,235,229,0.16)` |
| radius | `0` |
| accent blue | none |

1. Same nav as home. Playground fill `#080B08`, text `#FFDB2A`, height `48px`.
2. Page below the nav is `#0E1312`. Heading `Examples` `44px/700` paper. Sub `15px` muted. Max-width `1200px`. Side pad `24px`.
3. Filters: type, then theme. Chip height `44px`, radius `0`, `11px` mono uppercase. Idle: transparent, `1px` paper at 28%. Active: fill `#FFDB2A`, text ink. No pills.
4. Grid: `1` column under `768px`, `2` from `768px`, `4` from `1200px`. Gap `16px`. No `3`-column step.
5. Card: paper `#EDEBE5`, radius `0`, padding `12px`. Thumb SVG `width:100%`, `height:auto`, uncropped. Caption only if the SVG title is not already visible.
6. Detail rail: paper cell, radius `0`, uncropped SVG, fence, Open in Play.
7. Footer `#F8F8F6`, same as home.

390: one column. Filters wrap. Detail stacks. No horizontal page scroll.

## Acceptance

- Same nav and footer as `/`.
- Dark page, paper cards, square chips, `1 / 2 / 4`.
- A card never crops the plot.
