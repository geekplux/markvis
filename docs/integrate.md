# Integrate

VitePress, Astro, and markdown-it: see `examples/hosts/`.

Docusaurus: skipped for B5/B6 (MDX + plugin surface is not cheap). Use `markvis bake` or the browser drop-in until a dedicated adapter lands.
