# shadcn

Rounded marks, categorical chart-1..5 hues, card-quiet axes. Tokens only — no shadcn/ui or Recharts deps.

Source of truth: `theme.ts`. Fence id: `shadcn`.

See `docs/themes.md` for locked tokens and ban list.
