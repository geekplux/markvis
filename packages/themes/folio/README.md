# folio

Ledger editorial default: transparent canvas, hairline grid, value labels when dual-encoding allows, one accent for one series.

Source of truth: `theme.ts`. Fence id: `folio` (also the omit default).

See `docs/themes.md` for locked tokens and ban list.
