# highcharts

Denser plot, stronger grid, legend-friendly series chrome. Highcharts-demo-inspired look as tokens only — no vendor deps.

Source of truth: `theme.ts`. Fence id: `highcharts`.

See `docs/themes.md` for locked tokens and ban list.
