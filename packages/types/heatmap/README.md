# heatmap

Use for two discrete categories (x and `series`) with a numeric intensity (`y`). Long form only: one row per cell. Keep first-seen category order. `series` is required.

Do not use for a single category comparison (`bar`) or a continuous scatter cloud (`scatter`).
