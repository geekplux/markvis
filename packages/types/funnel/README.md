# funnel

Use for ordered stages with a non-negative count (`y ≥ 0`). Keep input stage order. `series` is ignored. Negatives fail with `E_NEGATIVE_VALUE`.

Do not use for part-to-whole slices (`pie`) or signed running totals (`waterfall`).
