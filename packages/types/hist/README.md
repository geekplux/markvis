# hist

Use when x is a numeric sample to bin (Sturges equal-width). Optional y is sample weight. The table keeps raw rows.

Do not use for pre-aggregated categories (`bar`) or unlabeled continuous series (`line`).
