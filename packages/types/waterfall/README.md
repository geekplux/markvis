# waterfall

Use for ordered steps with a signed delta (`y`). Paint a running baseline; keep input order. `series` is ignored.

Do not use for independent category comparisons (`bar`) or non-negative stage drop-off (`funnel`).
