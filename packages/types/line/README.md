# line

Use for ordered x with numeric y — trends over time or sequenced categories. Optional `series` draws multiple strokes.

Do not use for unordered category comparison (`bar`), filled bands (`area`), or raw sample distributions (`hist`).

Optional `layout: grouped | stacked | percent` (Wave 1). Stacked/percent use cumulative y.
